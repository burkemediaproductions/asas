
(()=>{const body=document.body,toggle=document.querySelector('[data-menu-toggle]'),menu=document.querySelector('[data-mobile-menu]');function setMenu(open){if(!toggle||!menu)return;toggle.setAttribute('aria-expanded',String(open));menu.classList.toggle('open',open);menu.setAttribute('aria-hidden',String(!open));body.classList.toggle('menu-open',open);if(open){setTimeout(()=>menu.querySelector('a')?.focus(),20)}}toggle?.addEventListener('click',()=>setMenu(toggle.getAttribute('aria-expanded')!=='true'));document.addEventListener('keydown',e=>{if(e.key==='Escape')setMenu(false)});menu?.addEventListener('click',e=>{if(e.target.matches('a'))setMenu(false)});
const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in-view');io.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));
if(!matchMedia('(prefers-reduced-motion: reduce)').matches){const els=[...document.querySelectorAll('.parallax img')];let ticking=false;function update(){const h=innerHeight;els.forEach(img=>{const r=img.parentElement.getBoundingClientRect();const p=(r.top-h)/(h+r.height);img.style.transform=`translateY(${Math.max(-20,Math.min(20,p*30))}px) scale(1.06)`});ticking=false}addEventListener('scroll',()=>{if(!ticking){requestAnimationFrame(update);ticking=true}},{passive:true});update()}
})();
